<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='css/board.css' />
  </head>
  <body>

    <% if(username){ %>
    <h1><%= title %></h1>
    <table border="2">
      <tr>
        <th>번호</th>
        <th>제목</th>
        <th>글쓴이</th>
        <th>수정일</th>
        <th>수정</th>
        <th>삭제</th>
      </tr>
      <%
      for(var i = 0; i<rows.length; i++)
      {
      %>
      <tr>
        <td><%=rows[i].idx%></td>
        <td><a href="/boardlist/read/<%=rows[i].idx%>"><%=rows[i].title%></a></td>
        <td><%=rows[i].writer%></td>
        <td><%=rows[i].moddate%></td>
        <td><button> <%=rows[i].idx%> 수정 </button></td>
        <td><input type="checkbox"></td>
      </tr>
      <%
      }
      %>
    </table>
    <div><a href="/board/write"> 글쓰기 </a></div>

    
    <% }else{ %>
        <h1> Admin Only😀 </h1>
        <button class="login_button" id="loginButton"> <a href="/login"> Admin Login </a> </button>
      <% } %>

  </body>
</html>